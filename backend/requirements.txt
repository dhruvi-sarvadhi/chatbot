fastapi>=0.118
uvicorn[standard]>=0.36
pydantic>=2.12
pydantic-settings>=2.11

# Persistence — conversations, messages and per-turn metrics in PostgreSQL.
# psycopg is the v3 driver; [binary] ships the compiled libpq, so there is
# nothing to build locally.
sqlalchemy>=2.0.36
psycopg[binary]>=3.2

# LLM providers — you only need the one you set as LLM_PROVIDER in .env
anthropic>=0.69
openai>=1.109
ddgs>=9.15
tavily-python>=0.5

# Clarix Projects API — its responses are AES-256-GCM encrypted end to end
# (ENABLE_PAYLOAD_ENCRYPTION), so a client needs a real AES implementation just
# to READ them. Python's stdlib has no AES. See app/clarix_client.py.
cryptography>=43
